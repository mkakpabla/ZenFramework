{% extends 'layouts/admin.twig' %}
{% block content %}

    <div class="container">
        <div class="card">
            <div class="card-header bg-primary" style="color: #FFF">
                Administration du blog
            </div>
            <div class="card-body">
                <a class="mb-2" href="{{ route('admin.posts.index') }}" style="display: block">Gerer les articles</a>
                <a href="{{ route('admin.categories.index') }}">Gerer les catégories</a>
            </div>
        </div>
    </div>
{% endblock %}

{% block script %}

{% endblock %}