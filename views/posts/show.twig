{% extends 'layouts/app.twig' %}

{% block content %}

    <div class="container">
        <div class="row">
            <h2>{{ post.title }}</h2>
            <p>{{ post.content }}</p>
        </div>
    </div>

{% endblock %}