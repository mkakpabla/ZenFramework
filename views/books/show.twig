{% extends('layouts/app.twig') %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>{{ book.title}}</h1>
                <p>{{ book.summary }}</p>
            </div>
        </div>
    </div>
{% endblock %}