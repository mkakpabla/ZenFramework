{% extends('layouts/app.twig') %}

{% block content %}
    <div class="container mx-auto py-4">
        <h1 class="text-2xl uppercase">{{ book.title}}</h1>
        <p>{{ book.summary }}</p>
    </div>
{% endblock %}